

# ROS 2 / ロボコン用 Dual Robot Controller リファクタリング方針

## 目的

既存コードの動作・操作感を大きく変えずに、以下を改善する。

- 初心者でもコードを読みやすくする
- 変更・保守をしやすくする
- ログを見たときに現場トラブルの原因を追いやすくする
- Joyコントローラーの接続が切れた場合は停止する
- `update_robot_velocity()` の責務を少し整理する
- 重複しているcallbackを整理する
- コードを過度にクラス化・分割しない

## 前提・設計方針

このコードはロボコンで使用する比較的小規模なシステムである。

ハードウェア側に緊急停止機構があるため、ソフトウェア側で過剰に複雑な安全機構を導入する必要はない。

また、初心者も開発に参加するため、

- ROS 2で必要な `Node` クラスなどは使用する
- それ以外のクラスは原則として増やさない
- `dataclass` などへの全面的な移行はしない
- 複数ファイル・複数クラスへの過度な分割はしない
- 1ファイルを読めば全体の流れがある程度分かる構成を維持する

こと。

今回のリファクタリングでは「綺麗な設計」よりも、


> 初心者が読んで理解しやすいこと

を重視する。

---

# 1. `robot_a` / `robot_b` の辞書は維持する

現在の

```python
robot_a = {
    ...
}

robot_b = {
    ...
}
````

という辞書構造は維持する。

クラス化・`dataclass` 化は今回行わない。

また、辞書であることを明示したいため、変数名・引数名には `robot_dict` を使用する。

例:

```python
def update_robot_velocity(robot_dict, joy_msg, logger):
```

`robot` ではなく `robot_dict` とする。

## 配列の意味をコメントで明示する

以下のような配列について、各要素が何を意味するのかコメントを明確にする。

```python
'dxl_ids': [1, 2, 3],      # [X軸, Y1軸, Y2軸]
'invert': [1, -1, 1],      # [X軸, Y1軸, Y2軸]
'target_vel': [0, 0, 0],   # [X軸速度, Y1軸速度, Y2軸速度]
```

このコードでは配列の順番を

```text
0番目 = X
1番目 = Y1
2番目 = Y2
```

として統一する。

配列をクラスや複雑な型に置き換える必要はない。

---

# 2. `update_robot_velocity()` を少し分割する

現在の `update_robot_velocity()` は、

- Joy接続確認
    
- エアシリンダーのトグル
    
- 真空ポンプのトグル
    
- 緊急停止
    
- Slow Mode判定
    
- 速度計算
    
- ログ出力
    

をすべて担当している。

これを多少分割する。

ただし、分割しすぎない。

候補として以下程度にする。

```python
update_robot_buttons(...)
update_slow_mode(...)
update_robot_velocity(...)
```

または、より分かりやすい名前があればそれを採用してよい。

重要なのは、

```text
Joy callback
    ↓
ボタン処理
    ↓
Slow Mode処理
    ↓
速度計算
```

という流れがコード上でも上から読めるようにすること。

細かい処理を大量の関数に分割しないこと。

---

# 3. callbackの重複をなくす

現在、

```python
def joy_a_callback(self, msg):
    update_robot_velocity(robot_a, msg, self.get_logger())

def joy_b_callback(self, msg):
    update_robot_velocity(robot_b, msg, self.get_logger())
```

となっている。

`robot_a` / `robot_b` それぞれ専用のcallback関数を持つ必要がない構造に変更する。

例えば共通callbackを使い、

```python
def joy_callback(self, robot_dict, msg):
    ...
```

のようにするなど、重複を減らす。

ただし、初心者が読みにくくなるような高度なテクニックは使わない。

「AとBのcallbackがほぼ同じなので共通化した」と読んで分かる程度の実装にする。

---

# 4. Joy接続監視を改善する

現在の

```python
'joy_connected': False
```

は「一度でもJoyを受信したか」を表すだけであり、接続断を検出できない。

今回、

> Joyコントローラーからの入力が一定時間来なかったら停止する

仕様にする。

そのため、辞書に例えば、

```python
'last_joy_time': None
```

を追加する。

Joy callbackでJoyを受信するたびに最終受信時刻を更新する。

そして `check_joy_health()` で、

```text
最後のJoy受信から一定時間以上経過
    ↓
接続切れと判定
    ↓
target_vel = [0, 0, 0]
```

とする。

タイムアウト時間は定数として定義する。

例:

```python
JOY_TIMEOUT = 0.5
```

など。

ただし、実際の値は元コードの周期やロボコンでの操作感を考慮して妥当な値を設定すること。

## 重要

接続切れ時には、

```python
robot_dict['target_vel'] = [0, 0, 0]
```

として、最後の速度指令を出し続けないようにする。

接続が切れた状態で毎回大量のWARNログを出さないようにする。

「接続切れを検出した瞬間」など、状態が変化したタイミングでログを出す。

また、Joyが再び届いたときには、

```text
[robot_a] Joy connected
```

など、復旧したことが分かるログを出す。

---

# 5. `joy_connected` の扱いを見直す

`joy_connected` を完全に削除する必要はない。

ただし、

```text
一度もJoyを受信していない
```

と

```text
一度は受信したが、現在はタイムアウトしている
```

を区別できるようにする。

例えば、

- `joy_connected`
    
- `last_joy_time`
    

の組み合わせでもよい。

重要なのは、

```text
未接続
接続中
接続断
再接続
```

をログ・状態として追えること。

---

# 6. ログを改善する

ログは「プログラム内部の情報」ではなく、

> 現場でトラブルが起きたときに、人間がログを見て原因を追えるもの

を目標とする。

## ログの基本方針

### INFO

通常の状態変化・ユーザー操作。

例:

```text
[robot_a] Joy connected
[robot_a] Air Cylinder: ON
[robot_a] Air Cylinder: OFF
[robot_a] Vacuum Pump: ON
[robot_a] Speed Mode: SLOW
[robot_a] Speed Mode: NORMAL
```

### WARN

通常ではないが、プログラムを継続できる状態。

例:

```text
[robot_a] Joy timeout: stopping robot
```

### ERROR

ハードウェア通信など、明確な異常。

例:

```text
[Dynamixel ID=4] Failed to write operating mode
```

### DEBUG

開発時に確認したい細かい情報。

例:

```text
[robot_a] Target velocity: [100, 0, -40]
```

---

# 7. ログに時刻を入れ、読みやすくする

現場でトラブル解析しやすいように、ログの時刻が分かるようにする。

可能ならROS 2のloggerの標準的なタイムスタンプ表示を活用する。

ログ本文は過剰に機械的な形式にせず、人間が読みやすい形式を優先する。

例えば、

```text
[INFO] [robot_a] Vacuum Pump: ON
[WARN] [robot_a] Joy timeout (0.62 sec): stopping robot
[ERROR] [Dynamixel ID=4] Failed to set operating mode
```

のような形式を基本とする。

「対象」「何が起きたか」「必要なら原因・結果」が分かることを重視する。

---

# 8. 速度ログは出しすぎない

現在、スティック入力がある間はDebugログを出している。

高頻度ログになりすぎる可能性があるため、必要に応じて、

```text
速度が変化したときだけ出す
```

などの改善を検討する。

ただし、まずはコードを複雑にしないことを優先する。

Debugログ自体を完全に削除する必要はない。

---

# 9. Whyコメントを増やす

コードを見れば分かる「何をしているか」だけではなく、

> なぜその実装になっているのか

をコメントする。

特に以下のような部分にはWhyコメントを入れる。

## PS4トリガー

現在、

```python
joy_msg.axes[AXIS_L2] < -0.5
```

などで判定している。

これはPS4系のJoyメッセージにおける未入力・押し込み時の値に依存するため、

```python
# PS4ではL2/R2の未入力値が1.0で、
# 押し込むと値が小さくなるため、この条件で押下判定する。
```

など、「なぜこの判定値なのか」を説明する。

## Dynamixelの速度を4バイトに変換する部分

例えば、

```python
vel_bytes = [(vel >> (8 * i)) & 0xFF for i in range(4)]
```

には、

```python
# Dynamixel SDKへ32bitの速度値を4バイトに分解して渡す。
```

など、この変換が必要な理由をコメントする。

## その他

「この値は仕様上こうなっている」「この順番にする必要がある」など、コードだけでは判断できない理由をコメントする。

---

# 10. Magic Numberを減らす

現在の

```python
MAX_VELOCITY * 0.5
```

の `0.5` など、意味のある数値には名前を付ける。

例えば、

```python
SLOW_MODE_RATIO = 0.5
```

とする。

そのうえで、

```python
speed_limit = MAX_VELOCITY * SLOW_MODE_RATIO
```

とする。

同様に、Joy timeoutなども定数化する。

ただし、すべての数値を無理に定数化する必要はない。

「この数値には意味がある」と判断できるものを対象とする。

---

# 11. Joyのaxis/button取得処理を必要に応じて共通化する

現在は、

```python
if len(joy_msg.buttons) > BUTTON_CIRCLE:
```

や

```python
if len(joy_msg.axes) > AXIS_RIGHT_STICK_Y:
```

のような処理が複数存在する。

必要なら、

```python
get_button(...)
get_axis(...)
```

のような小さなヘルパー関数を作ってもよい。

例:

```python
def get_button(joy_msg, index):
    if len(joy_msg.buttons) > index:
        return joy_msg.buttons[index]
    return 0
```

```python
def get_axis(joy_msg, index):
    if len(joy_msg.axes) > index:
        return joy_msg.axes[index]
    return 0.0
```

ただし、これによって逆にコードが分かりにくくなる場合は無理に導入しない。

---

# 12. Dynamixel初期化時のエラー確認を追加する

現在、以下の処理について戻り値を十分に確認していない。

```python
packet_handler.write1ByteTxRx(
    port_handler,
    dxl_id,
    ADDR_TORQUE_ENABLE,
    TORQUE_DISABLE
)

packet_handler.write1ByteTxRx(
    port_handler,
    dxl_id,
    ADDR_OPERATING_MODE,
    VELOCITY_MODE
)

packet_handler.write1ByteTxRx(
    port_handler,
    dxl_id,
    ADDR_TORQUE_ENABLE,
    TORQUE_ENABLE
)
```

これらについて、通信結果およびDynamixel側のエラーを確認する。

例えば、

```text
ID=4
操作=Operating Mode設定
結果=通信エラー
```

など、どのIDのどの操作が失敗したか分かるログを出す。

ここもクラス化などの大きな変更は必要ない。

---

# 13. `timer_callback()` は大きく変更しない

現在の `timer_callback()` は、

```text
robotのtarget_velを取得
↓
Dynamixel用4byteへ変換
↓
GroupSyncWriteへ登録
↓
6軸まとめて送信
```

という明確な処理になっている。

この処理は分割しすぎない。

必要なWhyコメントを追加する程度でよい。

特に、

```python
vel_bytes = [(vel >> (8 * i)) & 0xFF for i in range(4)]
```

の理由をコメントする。

また、`addParam()` や `txPacket()` の失敗時には、原因を追えるログを出す。

---

# 14. `main()` は必要以上に分割しない

現在のmainは、

- ROS初期化
    
- Dynamixel接続
    
- Baudrate設定
    
- Dynamixel初期化
    
- Node生成
    
- spin
    
- 終了処理
    

を担当している。

今回の方針では、これを大量の関数・クラスへ分割する必要はない。

ただし、

- ポートオープン失敗
    
- Baudrate設定失敗
    
- Dynamixel初期化失敗
    

などのエラーについて、原因が分かるようにする。

---

# 15. 終了処理の安全性は維持する

現在の終了処理では、

```text
全軸速度0
↓
Torque OFF
↓
Port close
↓
Node destroy
↓
rclpy.shutdown
```

という流れになっている。

この考え方は維持する。

正常終了・Ctrl+Cなどで終了するときに、速度0を送信してからTorque OFFする処理を壊さない。

---

# 16. 今回は導入しないもの

以下は今回は行わない。

- `dataclass` への全面移行
    
- Robotクラスの追加
    
- SafetyControllerクラスの追加
    
- DynamixelDriverクラスの追加
    
- Commandクラスの追加
    
- 複数ファイルへの大規模分割
    
- 複雑な状態機械
    
- 過度な抽象化
    
- デザインパターンの導入
    

今回の優先順位は、

> 読みやすさ > 保守しやすさ > ログの分かりやすさ > 過度な抽象化を避ける

とする。

---

# 17. 最終的に目指すコードの構造

大まかには以下の構造を維持する。

```text
定数定義
↓
robot_a / robot_b の辞書
↓
小さな共通関数
  - apply_deadzone()
  - 必要なら get_axis()
  - 必要なら get_button()
↓
Joy入力処理
  - ボタン処理
  - Slow Mode処理
  - 速度計算
↓
DualRobotController (ROS 2 Node)
  - Joy callback
  - Joy health check
  - Dynamixel timer
↓
main()
  - 初期化
  - ROS spin
  - 終了処理
```

この構造を大きく崩さない。

---

# 18. 最重要の変更点

今回のリファクタリングで特に重要なのは以下。

1. `update_robot_velocity()` を適度に分割する
    
2. A/BのJoy callbackの重複をなくす
    
3. `robot_dict` という命名を維持する
    
4. 配列の意味をコメントで明示する
    
5. Whyコメントを増やす
    
6. ログを読みやすくする
    
7. ログで時刻・対象・イベント・原因が追えるようにする
    
8. Joy timeoutを導入し、接続断時は速度0にする
    
9. Dynamixel初期化時の通信エラーを確認する
    
10. 意味のあるMagic Numberを定数化する
    
11. コードをクラス化・分割しすぎない
    
12. 既存の基本的な操作仕様を極力維持する
    

---

# 19. リファクタリング時の注意

既存コードを全面的に書き換えるのではなく、

> 「動作を維持したまま、読みやすさと保守性を改善する」

ことを優先する。

特に以下の操作仕様は勝手に変更しない。

- X/Y1/Y2の軸対応
    
- `invert` の意味
    
- PS4のボタン・軸番号
    
- Slow Modeの動作
    
- シリンダーのトグル動作
    
- ポンプのトグル動作
    
- E-STOPの動作
    
- DynamixelのID
    
- Baudrate
    
- `/joy_a`, `/joy_b`
    
- 20Hz送信周期
    

変更した場合は、何を変更したかを明示する。

---

# 20. 実装後に確認したいこと

リファクタリング後は、少なくとも以下を確認する。

### Joy操作

- 左スティックX → X軸が正しく動く
    
- 左スティックY → Y1軸が正しく動く
    
- 右スティックY → Y2軸が正しく動く
    
- `invert` が従来と同じ
    
- Deadzoneが従来と同じ
    
- L2/R2でSlow Modeになる
    
- ○ボタンでシリンダーがトグルする
    
- ×ボタンでポンプがトグルする
    
- OPTIONSでE-STOPになる
    

### 接続監視

- Joy未接続状態をログで確認できる
    
- Joy接続時にログが出る
    
- Joyが一定時間来なくなったら停止する
    
- Joy再接続時に復帰をログで確認できる
    

### ハードウェア

- Dynamixel初期化失敗時に原因が分かる
    
- `addParam()` 失敗時にIDが分かる
    
- `txPacket()` 失敗時に通信エラーが分かる
    
- 終了時に全軸速度0 → Torque OFFになる
    

### ログ

ログだけを見ても、

- いつ
    
- どのRobotで
    
- 何が起きて
    
- どういう状態になったか
    

をある程度追えること。

---

## リファクタリングの基本方針

**「難しいことをする」のではなく、「今のコードを初心者でも読みやすくする」。**

クラスや抽象化を増やすこと自体を目的にしない。

コードを見たときに、

```text
Joyを受け取る
↓
ボタンを処理する
↓
Slow Modeを処理する
↓
速度を計算する
↓
20HzでDynamixelへ送る
```

という流れがすぐ分かることを重視する。

また、コメントは「何をしているか」よりも「なぜそうしているか」を優先して追加する。
